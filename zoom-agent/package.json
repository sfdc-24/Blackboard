{
  "name": "sfdc24-meeting-agent",
  "version": "0.1.0",
  "private": true,
  "description": "SFDC24 Consultant — real-time AI assistance on Zoom client calls via RTMS",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test test/*.test.mjs",
    "selftest": "node test/simulate-call.mjs"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ws": "^8.18.0"
  },
  "optionalDependencies": {
    "@zoom/rtms": "^1.0.0"
  }
}
