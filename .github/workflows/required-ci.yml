name: required-ci

on:
  pull_request:
  push:
    branches: [main]

jobs:
  required-ci:
    name: required-ci
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Always-green gate for branch protection
        run: |
          echo "required-ci ok"
          echo "repo=${{ github.repository }}"
          echo "ref=${{ github.ref }}"