# Website backend integration with the reviewed staging build

PR #4 targets `session/vm-cicd` and is synchronized through owner-lane commit `9944fa71a07a48bce83c94084ef9808714174fa9`, so it uses the repaired staging pipeline and reconciled Governor v31 source without duplicating that review.

The independent source read-back and seven v31 baseline hashes are in [CICD-GOVERNOR-V31.md](CICD-GOVERNOR-V31.md). This integration retains v30's `TTS_ENABLED` kill switch, degraded-reply restriction and timestamp/alarm changes, plus v31's failed-mail retry behavior and explicit scope manifest. It replaces the cache-only claim implementation with PR #4's property-backed claim and independent site/session audio budgets. The obsolete cache-only helper is removed. Reception retains its application-ready nonce handshake; the build-health route remains independent of visitor chat or paid providers.

The local combined suite has 107 passing tests: 98 CI/CD/source/auth/monitor cases plus nine voice/readiness cases. The eight v31 cases first failed on the older review source, then passed after reconciliation. They cover failed-mail retries, daily-cap rollover and explicit scope preservation. The staging cases now prove an unreachable app stops before `clasp push`, and that only real HTTP applications with reviewed health contracts are selectable. Two voice cases verify that disabling TTS prevents new audio keys and blocks provider calls for keys issued before the switch was disabled. All provider requests and Google API/project methods used by tests are mocked. The voice workflow uses verified commit pins for checkout/setup-node and disables persisted checkout credentials.

Review PR #4 against the current VM owner lane, then adopt through that lane. The source/build protocol must produce real staging deploy/rollback receipts, and the paired static-site behavior must be accepted on staging before production. Green offline tests and the owner-consent bootstrap response are not live build acceptance. No production source, deployment configuration, Google credentials, Salesforce records or DNS changed during this integration.
